import { NotificationContext, useNotifications, NotificationContextType } from './context';

// Export the context and hooks
export {
  NotificationContext,
  useNotifications
};

// Export types separately with 'export type'
export type { NotificationContextType };
