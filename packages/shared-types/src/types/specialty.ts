export interface Specialty {
  id: number;
  name: string;
  description?: string;
  createdAt?: string;
  updatedAt?: string;
}

export interface SpecialtyData {
  name: string;
  description?: string;
}
